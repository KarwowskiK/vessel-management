import { Route } from '@angular/router';
import { EmissionsComponent } from './emissions/emissions.component';

export const emissionsRoutes: Route[] = [
  { path: '', component: EmissionsComponent },
];
